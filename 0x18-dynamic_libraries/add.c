#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers
 * @a: first integer
 * @b: second integer
 * Return: the result of the addition
 */

int add(int a, int b)
{
    return (a + b);
}
