#include "main.h"
#include <stdio.h>

/**
 * div - divides two integers
 * @a: first integer
 * @b: second integer
 * Return: the result of the division
 */

int div(int a, int b)
{
return (a / b);
}
