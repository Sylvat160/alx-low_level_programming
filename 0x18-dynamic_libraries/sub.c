#include "main.h"
#include <stdio.h>

/**
 * sub - substracts two integers
 * @a: first integer
 * @b: second integer
 * Return: the result of the substraction
 */

int sub(int a, int b)
{
return (a - b);
}
