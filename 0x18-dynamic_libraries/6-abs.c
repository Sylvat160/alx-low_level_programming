#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @t: the int to check
 * Return: the absolute value of int esdfsd
 */

int _abs(int t)
{
if (t >= 0)
{
return (t);
}
else
{
return (-t);
}
}
